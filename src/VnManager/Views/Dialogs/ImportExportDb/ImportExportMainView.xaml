<controls:AdonisWindow x:Class="VnManager.Views.Dialogs.ImportExportDb.ImportExportMainView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:local="clr-namespace:VnManager.Views.Dialogs.ImportExportDb"
                       xmlns:res="clr-namespace:VnManager.Strings"
                       xmlns:s="https://github.com/canton7/Stylet"
                       xmlns:controls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                       mc:Ignorable="d"
                       Title="{x:Static res:Resources.ImportDataTitle}" Height="350" Width="500" MinHeight="350" MinWidth="500">
    <Grid>
        <TabControl>
            <TabItem Header="{x:Static res:Resources.ExportData}">
                <ContentControl s:View.Model="{Binding ExportTab}"/>
            </TabItem>
            <TabItem Header="{x:Static res:Resources.ImportData}">
                <ContentControl s:View.Model="{Binding ImportTab}"/>
            </TabItem>
        </TabControl>
    </Grid>
</controls:AdonisWindow>
