<UserControl x:Class="VisualNovelManagerv2.Pages.Links.About.Licenses"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualNovelManagerv2.Pages"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500"
             >
    <Grid>
        <Grid>
            <DataGrid Name="LicenseDataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserResizeColumns="False" CanUserReorderColumns="False"
                      CanUserSortColumns="False" IsReadOnly="True" >
                <DataGrid.Columns>
                    <mui:DataGridTextColumn Header="Software"  Binding="{Binding Software}"/>
                    <mui:DataGridTextColumn Header="Info" Binding="{Binding Info}" />
                    <mui:DataGridTextColumn Header="Url" Binding="{Binding Url}"/>
                    <mui:DataGridTextColumn Header="License" Binding="{Binding License}" />

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
